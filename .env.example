# Copy this file to `.env` and fill in real values.
# Format: KEY=value (phpdotenv-compatible). If you prefer shell format,
# you can also use `export KEY=value` (index.php supports both).

# App environment for CLI runs (development|testing|production)
CI_ENV=development

# Email (CodeIgniter Email library)
SMTP_PROTOCOL=smtp
SMTP_HOST=mail.kharjamaat.in
SMTP_PORT=465
SMTP_USER=admin@kharjamaat.in
SMTP_PASS=
SMTP_CRYPTO=ssl

# Enable ONLY if you explicitly want PHP mail() fallback when SMTP fails
EMAIL_ALLOW_PHP_MAIL_FALLBACK=0

# Prefer PHP mail() as PRIMARY transport (tries mail() first, then SMTP)
EMAIL_PREFER_PHP_MAIL=0

# Optional: allow running EmailWorker over web with a token
EMAILWORKER_DEBUG_TOKEN=
